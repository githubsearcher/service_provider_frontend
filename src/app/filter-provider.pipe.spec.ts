import { FilterProviderPipe } from './filter-provider.pipe';

describe('FilterProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
