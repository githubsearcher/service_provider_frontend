import { SortProviderPipe } from './sort-provider.pipe';

describe('SortProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
